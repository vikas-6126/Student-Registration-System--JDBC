# Student-Registration-System--JDBC
A simple Javaconsole application that connects to a MySQL database using JDBC It allows users to register students by entering their name, roll number, and department, and stores the data securely in a database. The project demonstrates basic JDBC operations like driver loading, connection creation, and secure data insertion with PreparedStatement.
