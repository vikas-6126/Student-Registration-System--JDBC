# Student-Registration-System--JDBC
Java console application implementing a Student Registration System with JDBC. Demonstrates driver loading via Class.forName(), secure DB connectivity using DriverManager, CRUD operations with PreparedStatement, and relational persistence in MySQL (students table). Showcases modular DAO pattern and SQL injection prevention.

